#include <iostream>
using namespace std;
int main()
{
	int i,num,sum=0;
	cout<<"enter the number:";
	cin>>num;
	for(int i=1;i<num;i++)
	{
		if(num%i==0)
		sum=sum+i;
	}
	if(sum==num)
	cout<<"number is perfect number."<<num;
	else
	cout<<"number is not a perfect number."<<num;
	return 0;
}
