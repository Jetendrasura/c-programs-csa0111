#include <iostream>
#include <cmath>
using namespace std;
int main()
{
	double num;
	 
	cout<<"enter the number:";
	cin>>num;
	
	if(num < 0)
	{
		cout<<"no root for negative number is undefined";
	}else{
		double result = sqrt(num);
		cout<<"sqrt of num is :"<<result<<endl;
	}
	return 0;
}
